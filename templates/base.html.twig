<!DOCTYPE html>
<html lang="en">
    <head>

        {# Tarte au citron (RGPD Cookies) #}
        {# // <script src="tarteaucitron/tarteaucitron.min.js"></script> #}
        <script type="text/javascript">
            tarteaucitron.init({
            "privacyUrl": "", /* Url de la politique de confidentialité */
            "bodyPosition": "top", /* top place le bandeau de consentement au début du code html, mieux pour l'accessibilité */

            "hashtag": "#tarteaucitron", /* Hashtag qui permet d'ouvrir le panneau de contrôle  */
            "cookieName": "tarteaucitron", /* Nom du cookie (uniquement lettres et chiffres) */
        
            "orientation": "middle", /* Position de la bannière (top - bottom - popup - banner) */
        
            "groupServices": true, /* Grouper les services par catégorie */
            "showDetailsOnClick": true, /* Cliquer pour ouvrir la description */
            "serviceDefaultState": "wait", /* Statut par défaut (true - wait - false) */
                            
            "showAlertSmall": false, /* Afficher la petite bannière en bas à droite */
            "cookieslist": false, /* Afficher la liste des cookies */
                            
            "closePopup": true, /* Afficher un X pour fermer la bannière */

            "showIcon": true, /* Afficher un cookie pour ouvrir le panneau */
            //"iconSrc": "", /* Optionnel: URL ou image en base64 */
            "iconPosition": "BottomRight", /* Position de l'icons: (BottomRight - BottomLeft - TopRight - TopLeft) */

            "adblocker": false, /* Afficher un message si un Adblocker est détecté */
                            
            "DenyAllCta" : true, /* Afficher le bouton Tout refuser */
            "AcceptAllCta" : true, /* Afficher le bouton Tout accepter */
            "highPrivacy": true, /* Attendre le consentement */
            "alwaysNeedConsent": false, /* Demander le consentement même pour les services "Privacy by design" */
                            
            "handleBrowserDNTRequest": false, /* Refuser tout par défaut si Do Not Track est activé sur le navigateur */

            "removeCredit": false, /* Retirer le lien de crédit vers tarteaucitron.io */
            "moreInfoLink": true, /* Afficher le lien En savoir plus */

            "useExternalCss": false, /* Mode expert : désactiver le chargement des fichiers .css tarteaucitron */
            "useExternalJs": false, /* Mode expert : désactiver le chargement des fichiers .js tarteaucitron */

            //"cookieDomain": ".my-multisite-domaine.fr", /* Optionnel: domaine principal pour partager le consentement avec des sous domaines */
                            
            "readmoreLink": "", /* Changer le lien En savoir plus par défaut */

            "mandatory": true, /* Afficher un message pour l'utilisation de cookies obligatoires */
            "mandatoryCta": false, /* Afficher un bouton pour les cookies obligatoires (déconseillé) */
        
            //"customCloserId": "", /* Optionnel a11y: ID personnalisé pour ouvrir le panel */
            
            "googleConsentMode": true, /* Activer le Google Consent Mode v2 pour Google ads & GA4 */
            "bingConsentMode": true, /* Activer le Bing Consent Mode pour Clarity & Bing Ads */

            "dataLayer": false, /* Envoyer un événement dans dataLayer avec le statut des services */
            "serverSide": false, /* Server side seulement, les tags ne sont pas chargé côté client */
            
            "partnersList": true /* Afficher le détail du nombre de partenaires sur la bandeau */
            });
        </script>
        {# > Youtube cookies #}
        <script>
            (tarteaucitron.job = tarteaucitron.job || []).push('youtube');
        </script>
        {# > Twitch cookies #}
        <script>
            (tarteaucitron.job = tarteaucitron.job || []).push('twitch');
        </script>
        {# > Google API #}
        <script>
            (tarteaucitron.job = tarteaucitron.job || []).push('jsapi');
        </script>

        <meta charset="UTF-8">
        <title>{% block title %}CritX{% endblock %}</title>
        {% block stylesheets %}

            {# > Favicon #}
            <link rel="apple-touch-icon" sizes="180x180" href="{{ asset('favicon/apple-touch-icon.png')}}">
            <link rel="icon" type="image/png" sizes="32x32" href="{{ asset('favicon/favicon-32x32.png')}}">
            <link rel="icon" type="image/png" sizes="16x16" href="{{ asset('favicon/favicon-16x16.png')}}">
            <link rel="manifest" href="{{ asset('favicon/site.webmanifest')}}">

            {# > Font Awesome #}
            <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css" 
            integrity="sha512-Evv84Mr4kqVGRNSgIGL/F/aIDqQb7xQ2vcrdIwxfjThSH8CSR7PBEakCr51Ck+w+/U6swU2Im1vVX0SVk9ABhg==" 
            crossorigin="anonymous" 
            referrerpolicy="no-referrer" />

            {# > MyCSS #}
            <link rel="stylesheet" href="{{ asset('styles/style.css')}}">

        {% endblock %}
    </head>
    <body>
        <div class="container">
            <nav>
                <div id="flashMessages">
                    {# > Add/Edit Piece #}
                    {% for message in app.flashes('piAddEditSuccess') %}
                        <div class="alert alert-success">
                            {{message}}
                        </div>
                    {% endfor %}

                    {# > Delete Piece #}
                    {% for message in app.flashes('piDeleteSuccess') %}
                        <div class="alert alert-success">
                            {{message}}
                        </div>
                    {% endfor %}

                    {# > Add/Edit Critic #}
                    {% for message in app.flashes('crAddEditSuccess') %}
                        <div class="alert alert-success">
                            {{message}}
                        </div>
                    {% endfor %}

                    {# > Delete Critic #}
                    {% for message in app.flashes('crDeleteSuccess') %}
                        <div class="alert alert-success">
                            {{message}}
                        </div>
                    {% endfor %}

                    {# > Add/Edit Influencer #}
                    {% for message in app.flashes('inAddEditSuccess') %}
                        <div class="alert alert-success">
                            {{message}}
                        </div>
                    {% endfor %}

                    {# > Delete Influencer #}
                    {% for message in app.flashes('inDeleteSuccess') %}
                        <div class="alert alert-success">
                            {{message}}
                        </div>
                    {% endfor %}

                    {# > Search Influencer fail #}
                    {% for message in app.flashes('inSearchFail') %}
                        <div class="alert alert-danger">
                            {{message}}
                        </div>
                    {% endfor %}

                    {# > Search Category fail #}
                    {% for message in app.flashes('caSearchFail') %}
                        <div class="alert alert-danger">
                            {{message}}
                        </div>
                    {% endfor %}

                    {# > Search Critics fail #}
                    {% for message in app.flashes('crSearchFail') %}
                        <div class="alert alert-danger">
                            {{message}}
                        </div>
                    {% endfor %}

                    {# > Search Piece fail #}
                    {% for message in app.flashes('piSearchFail') %}
                        <div class="alert alert-danger">
                            {{message}}
                        </div>
                    {% endfor %}

                    {# > Register success #}
                    {% for message in app.flashes('registerSuccess') %}
                        <div class="alert alert-success">
                            {{message}}
                        </div>
                    {% endfor %}

                    {# > Email verify success #}
                    {% for message in app.flashes('verifySuccess') %}
                        <div class="alert alert-success">
                            {{message}}
                        </div>
                    {% endfor %}

                    {# > Search User fail #}
                    {% for message in app.flashes('usSearchFail') %}
                        <div class="alert alert-danger">
                            {{message}}
                        </div>
                    {% endfor %}

                    {# > User profile update success #}
                    {% for message in app.flashes('usEditSuccess') %}
                        <div class="alert alert-success">
                            {{message}}
                        </div>
                    {% endfor %}

                    {# > User profile update fail #}
                    {% for message in app.flashes('usEditFail') %}
                        <div class="alert alert-danger">
                            {{message}}
                        </div>
                    {% endfor %}

                    {# > User profile delete success #}
                    {% for message in app.flashes('usDeleteSuccess') %}
                        <div class="alert alert-success">
                            {{message}}
                        </div>
                    {% endfor %}

                    {# > User profile delete fail #}
                    {% for message in app.flashes('usDeleteFail') %}
                        <div class="alert alert-danger">
                            {{message}}
                        </div>
                    {% endfor %}

                    {# > Critic save success #}
                    {% for message in app.flashes('crSaveSuccess') %}
                        <div class="alert alert-success">
                            {{message}}
                        </div>
                    {% endfor %}

                    {# > Critic save fail #}
                    {% for message in app.flashes('crSaveFail') %}
                        <div class="alert alert-danger">
                            {{message}}
                        </div>
                    {% endfor %}

                    {# > Critic unsave success #}
                    {% for message in app.flashes('crUnSaveSuccess') %}
                        <div class="alert alert-success">
                            {{message}}
                        </div>
                    {% endfor %}

                    {# > Critic unsave fail #}
                    {% for message in app.flashes('crUnSaveFail') %}
                        <div class="alert alert-danger">
                            {{message}}
                        </div>
                    {% endfor %}

                    {# > Influencer like success #}
                    {% for message in app.flashes('inLikeSuccess') %}
                        <div class="alert alert-success">
                            {{message}}
                        </div>
                    {% endfor %}

                    {# > Influencer like fail #}
                    {% for message in app.flashes('inLikeFail') %}
                        <div class="alert alert-danger">
                            {{message}}
                        </div>
                    {% endfor %}

                    {# > Influencer unlike success #}
                    {% for message in app.flashes('inUnLikeSuccess') %}
                        <div class="alert alert-success">
                            {{message}}
                        </div>
                    {% endfor %}

                    {# > Influencer unlike fail #}
                    {% for message in app.flashes('inUnLikeFail') %}
                        <div class="alert alert-danger">
                            {{message}}
                        </div>
                    {% endfor %}

                    {# > User score vote success #}
                    {% for message in app.flashes('scPieceSuccess') %}
                        <div class="alert alert-success">
                            {{message}}
                        </div>
                    {% endfor %}

                    {# > User score vote fail #}
                    {% for message in app.flashes('scPieceFail') %}
                        <div class="alert alert-danger">
                            {{message}}
                        </div>
                    {% endfor %}

                    {# > User score reset fail #}
                    {% for message in app.flashes('scResetFail') %}
                        <div class="alert alert-danger">
                            {{message}}
                        </div>
                    {% endfor %}

                    {# > User score reset success #}
                    {% for message in app.flashes('scResetSuccess') %}
                        <div class="alert alert-success">
                            {{message}}
                        </div>
                    {% endfor %}

                    {# > User comment success #}
                    {% for message in app.flashes('coCriticSuccess') %}
                        <div class="alert alert-success">
                            {{message}}
                        </div>
                    {% endfor %}

                    {# > User comment fail #}
                    {% for message in app.flashes('coCriticFail') %}
                        <div class="alert alert-danger">
                            {{message}}
                        </div>
                    {% endfor %}

                    {# > User comment delete success #}
                    {% for message in app.flashes('coDeleteSuccess') %}
                        <div class="alert alert-success">
                            {{message}}
                        </div>
                    {% endfor %}

                    {# > User comment delete fail #}
                    {% for message in app.flashes('coDeleteFail') %}
                        <div class="alert alert-danger">
                            {{message}}
                        </div>
                    {% endfor %}

                    {# > VIP comment success #}
                    {% for message in app.flashes('vipSuccess') %}
                        <div class="alert alert-success">
                            {{message}}
                        </div>
                    {% endfor %}

                    {# > VIP comment fail #}
                    {% for message in app.flashes('vipFail') %}
                        <div class="alert alert-danger">
                            {{message}}
                        </div>
                    {% endfor %}

                    {# > Like critic success #}
                    {% for message in app.flashes('likCriticSuccess') %}
                        <div class="alert alert-success">
                            {{message}}
                        </div>
                    {% endfor %}

                    {# > Like critic fail #}
                    {% for message in app.flashes('likCriticFail') %}
                        <div class="alert alert-danger">
                            {{message}}
                        </div>
                    {% endfor %}

                    {# > Dislike critic success #}
                    {% for message in app.flashes('disCriticSuccess') %}
                        <div class="alert alert-success">
                            {{message}}
                        </div>
                    {% endfor %}

                    {# > Dislike critic fail #}
                    {% for message in app.flashes('disCriticFail') %}
                        <div class="alert alert-danger">
                            {{message}}
                        </div>
                    {% endfor %}

                </div>
            </nav>
        {% block body %}{% endblock %}
        </div>
        {% block javascripts %}

            {# > My JavaScript #}
            <script src="{{ asset('script.js') }}"></script>
            
            {# > Imports #}
            {% block importmap %}
                {# {{ importmap('app') }} #}
            {% endblock %}

        {% endblock %}
    </body>
</html>
