<!-- namespace App\Controller;

use Symfony\Bundle\FrameworkBundle\Controller\AbstractController;
use Symfony\Component\HttpFoundation\Response;
use Symfony\Component\Routing\Annotation\Route;

class APIkeysController extends AbstractController
{
    #Route('/page', name: 'page')
    public function index(): Response
    {
        // Récupérer la clé API définie dans .env.local
        $apiKey = $_ENV['API_YT_key'];  // Ici, tu récupères la valeur de la clé

        // Passer cette clé à ton template Twig
        return $this->render('page/index.html.twig', [
            'API_YT_key' => $apiKey,  // Tu transmets cette valeur à Twig sous le nom 'API_YT_key'
        ]);
    }
} -->



<!-- use Symfony\Component\DependencyInjection\ParameterBag\ParameterBagInterface;

class ApiService
{
    private $apiKey;

    public function __construct(ParameterBagInterface $params)
    {
        $this->apiKey = $params->get('API_KEY'); // Récupère la clé depuis .env
    }

    public function someMethod()
    {
        // Utiliser la clé API dans votre logique
        $apiKey = $this->apiKey;
    }
} -->



<!-- # config/services.yaml
parameters:
    api_key: '%env(API_KEY)%' -->



    <!-- console.log(window.apiKey); // Affiche la clé API dans la console

// Utilisez la clé API dans votre logique JavaScript
fetch('https://api.example.com/data', {
    method: 'GET',
    headers: {
        'Authorization': `Bearer ${window.apiKey}`,
    },
})
.then(response => response.json())
.then(data => console.log(data)); -->