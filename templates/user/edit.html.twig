{% extends 'base.html.twig' %}

{% block title %}
    CritX > Edit your profile
{% endblock %}

{% block body %}
    <h1>Edit profile</h1><br>
    
    {{ form_start(formAddUser) }}
    
        {{ form_row(formAddUser.status) }}<br>
        {{ form_row(formAddUser.bio) }}<br>
        {{ form_row(formAddUser.profilePicture) }}<br>

    {{ form_end(formAddUser) }}

{% endblock %}